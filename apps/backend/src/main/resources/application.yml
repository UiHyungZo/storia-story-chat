spring:
  application:
    name: backend
  datasource:
    url: jdbc:postgresql://localhost:5432/storia
    username: storia
    password: storia
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true

springdoc:
  swagger-ui:
    path: /swagger-ui.html
